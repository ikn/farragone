#! /usr/bin/python

# script for building and installing the Python package

from distutils.core import setup

setup(name='farragone', version='0.1.1', packages=[
    'farragone', 'farragone.core', 'farragone.ui', 'farragone.ui.qt'
])
